# tasks file
---
- name: repository | install | dependencies (pre)
  apt:
    name: "{{ percona_client_dependencies_pre }}"
    state: "{{ apt_install_state | default('latest') }}"
    update_cache: true
    cache_valid_time: "{{ apt_update_cache_valid_time | default(3600) }}"
  tags:
    - percona-client-repository-install
    - percona-client-repository-install-dependencies


- name: repository | download percona-keyring
  ansible.builtin.get_url:
    url: "{{ percona_client_asc_key }}"
    dest: "{{ percona_client_asc_path }}"
  changed_when: false
  tags:
    - percona-client-repository-public-key

- name: repository | add
  apt_repository:
    repo: "{{ item.type }} [signed-by={{ percona_client_asc_path }}] {{ item.url }} {{ item.component }}"
    state: present
    update_cache: true
  with_items: "{{ percona_client_repositories }}"
  tags:
    - percona-client-repository-add

- name: repository | apt-pin packages
  copy:
    src: etc/apt/preferences.d/00percona.pref
    dest: /etc/apt/preferences.d/00percona.pref
    owner: root
    group: root
    mode: 0644
  tags:
    - percona-client-repository-apt-pin
